<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:hardwareAccelerated="true"
        android:theme="@style/Theme.Finpro">

        <!-- StartActivity -->
        <activity
            android:name=".Activity.StartActivity"
            android:exported="true"
            android:resizeableActivity="true"
            android:supportsPictureInPicture="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- LoginActivity -->
        <activity
            android:name=".Activity.LoginActivity"
            android:exported="true" />

        <!-- RegisterActivity -->
        <activity
            android:name=".Activity.SignUpActivity"
            android:exported="true" />

        <!-- MainActivity -->
        <activity
            android:name=".Activity.MainActivity"
            android:exported="true" />

        <!-- Other Activities -->
        <activity android:name=".Activity.Detail_information" android:exported="false" />
        <activity android:name=".Activity.BookingServices" android:exported="false" />
        <activity android:name=".Activity.SummaryOrder" android:exported="false" />
        <activity android:name=".Activity.OrderSucced" android:exported="false" />
        <activity android:name=".Activity.ServiceType" android:exported="false" />
        <activity android:name=".Activity.ProfileActivity" android:exported="false" />
        <activity android:name=".Activity.ChatActivity" android:exported="false" />
        <activity android:name=".Activity.HomeServiceActivity" android:exported="false" />
        <activity android:name=".Activity.About" android:exported="false" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>
</manifest>
